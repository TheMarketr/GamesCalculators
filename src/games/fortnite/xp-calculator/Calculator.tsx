import {useMemo,useState} from 'preact/hooks';import {calculateFortniteXp} from './calculate';import {Field,ResultMetric,shareResult} from '../../../components/tools/CalculatorUi';import {formatNumber} from '../../../utils/format';
export default function Calculator(){const[current,setCurrent]=useState(1);const[target,setTarget]=useState(100);const[progress,setProgress]=useState(0);const[perLevel,setPerLevel]=useState(80000);const[perMatch,setPerMatch]=useState(28000);const result=useMemo(()=>calculateFortniteXp({currentLevel:current,targetLevel:target,currentLevelXp:progress,xpPerLevel:perLevel,xpPerMatch:perMatch}),[current,target,progress,perLevel,perMatch]);return <div class="calculator"><div class="calculator-header"><div><span class="eyebrow">Battle Pass progress</span><h2>Plan your target level</h2></div><button class="text-button" type="button" onClick={()=>{setCurrent(1);setTarget(100);setProgress(0);setPerLevel(80000);setPerMatch(28000)}}>Reset</button></div><div class="input-grid"><Field label="Current level"><input type="number" min="1" max="1000" value={current} onInput={(e)=>setCurrent(Number((e.target as HTMLInputElement).value))}/></Field><Field label="Target level"><input type="number" min={current} max="1000" value={target} onInput={(e)=>setTarget(Number((e.target as HTMLInputElement).value))}/></Field><Field label="XP already earned this level"><input type="number" min="0" max={perLevel} value={progress} onInput={(e)=>setProgress(Number((e.target as HTMLInputElement).value))}/></Field><Field label="Expected XP per match"><input type="number" min="1" value={perMatch} onInput={(e)=>setPerMatch(Number((e.target as HTMLInputElement).value))}/></Field></div><details class="advanced-settings"><summary>Season assumptions <span>+</span></summary><Field label="XP per level"><input type="number" min="1" value={perLevel} onInput={(e)=>setPerLevel(Number((e.target as HTMLInputElement).value))}/></Field></details><div class="primary-result primary-result--blue"><span>XP still needed</span><strong>{formatNumber(result.xpNeeded,0)} <small>XP</small></strong><div class="progress-track"><i style={{width:`${Math.min(100,current/target*100)}%`}}></i></div><p>{result.levelsRemaining} levels to go</p></div><div class="result-grid result-grid--three"><ResultMetric label="Estimated matches" value={formatNumber(result.matches,0)}/><ResultMetric label="Current-level progress" value={`${result.progressPercent.toFixed(0)}%`}/><ResultMetric label="XP per level" value={formatNumber(perLevel,0)}/></div><div class="calculator-actions"><button class="button button--primary" type="button" onClick={()=>shareResult('Fortnite XP plan',`Level ${current} to ${target}: ${formatNumber(result.xpNeeded,0)} XP, about ${result.matches} matches.`)}>Share result</button></div><p class="assumption-note">XP requirements and rewards can change by season. Edit XP per level and expected XP per match to match the current season and your modes.</p></div>}
