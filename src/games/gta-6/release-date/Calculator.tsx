import {useEffect,useMemo,useState} from 'preact/hooks';import {calculateCountdown,GTA_VI_RELEASE_ISO} from './calculate';import {shareResult} from '../../../components/tools/CalculatorUi';
export default function Calculator(){const[now,setNow]=useState(Date.now());useEffect(()=>{const timer=setInterval(()=>setNow(Date.now()),1000);return()=>clearInterval(timer)},[]);const result=useMemo(()=>calculateCountdown(now),[now]);return <div class="calculator countdown-calculator"><div class="calculator-header"><div><span class="eyebrow">Official release date</span><h2>November 19, 2026</h2></div><span class="confirmed-badge">✓ Confirmed</span></div><div class="countdown-grid" aria-live="off">{[['Days',result.days],['Hours',result.hours],['Minutes',result.minutes],['Seconds',result.seconds]].map(([label,value])=><div><strong>{String(value).padStart(2,'0')}</strong><span>{label}</span></div>)}</div><div class="release-summary"><span class="item-orb">VI</span><div><strong>{result.released?'GTA VI release day has arrived':'Coming to PlayStation 5 and Xbox Series X|S'}</strong><small>Countdown target: {new Date(GTA_VI_RELEASE_ISO).toLocaleString('en-US',{dateStyle:'full',timeStyle:'short'})}</small></div></div><div class="calculator-actions"><button class="button button--primary" type="button" onClick={()=>shareResult('GTA VI release countdown',`${result.days} days until GTA VI on November 19, 2026.`)}>Share countdown</button><a class="button button--secondary" href="https://www.rockstargames.com/newswire/article/ak3ak31a49a221/grand-theft-auto-vi-is-now-set-to-launch-november-19-2026" target="_blank" rel="noreferrer">Official announcement ↗</a></div><p class="assumption-note">The date is confirmed by Rockstar Games. The timer uses midnight Pacific Time as a clear date boundary; exact regional unlock times may differ.</p></div>}
