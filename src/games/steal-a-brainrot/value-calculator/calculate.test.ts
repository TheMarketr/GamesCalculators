import {describe,expect,it} from 'vitest';import {calculateBrainrotCollection,withIncomePeriods} from './calculate';
describe('Brainrot collection',()=>{it('applies quantity and mutation to value and income',()=>{const result=withIncomePeriods(calculateBrainrotCollection([{slug:'a',name:'A',category:'x',rarity:'x',value:100,demand:5,updated:'2026-08-26',lastReviewed:'2026-08-26',income:10,quantity:2,multiplier:5}]));expect(result).toMatchObject({value:1000,incomePerSecond:100,incomePerMinute:6000,incomePerHour:360000})})});
